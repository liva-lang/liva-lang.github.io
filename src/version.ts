// Single source of truth for the Liva compiler version surfaced in the site.
// Bump this on every release; do not hardcode the version anywhere else.

export const LIVA_VERSION = '2.7.2';

// Short marketing tag (e.g. shown next to the logo). Tracks the minor line.
export const LIVA_VERSION_SHORT = 'v2.7';

// Tagline shown next to the version badge in the hero. Update on each release.
export const LIVA_HERO_TAGLINE =
  'Self-hosted on every platform · Null-coalescing `??` · Jest tests';
